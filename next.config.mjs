/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //   runtime: 'experimental-edge',
    // },
    // other configurations...
  };
  
  export default nextConfig;